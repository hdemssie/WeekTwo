namespace RoutingApp.controllers {
    //Define a HomeController for the home state

    export class HomeController {
        public message: string = ` Hello, this is the message property on the home controller`;
    }

      //Define a HomeController for the about state
    export class AboutController {
        public message: string = `Hello, this the message property on about controller`;
    }
}